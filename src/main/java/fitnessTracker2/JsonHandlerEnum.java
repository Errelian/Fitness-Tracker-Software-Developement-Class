package fitnessTracker2;
/**
 * Simple enum for Json file Handling.
 */
public enum JsonHandlerEnum {
    /**
     * For the session.json files.
     */
    SESSION,
    /**
     * For the type.json files.
     */
    TYPE,
    /**
     * For the profile.json files.
     */
    PROFILE
}
